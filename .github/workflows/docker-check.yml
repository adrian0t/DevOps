name: Docker Build Check


on:
  push:
    branches: [ "main", "development" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Pobranie kodu (Checkout)
        uses: actions/checkout@v3

      - name: Próba zbudowania obrazu
        run: docker build . --file Dockerfile --tag my-test-image:latest
